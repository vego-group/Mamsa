# ── Vue SPA — STAGING build (testvue.mamsaa.com) ─────────────────
# Loaded by `vite build --mode staging` (npm run build:staging).
# Points the static build at the STAGING API, never production.
VITE_API_BASE_URL=https://staging.mamsaa.com/api/v1
