node_modules/
dist/
.env
.env.local
.env.*.local

# local build artifacts
testvue-dist.tar.gz
*.tar.gz

# Apple Pay domain-association files are environment-specific — never commit.
public/.well-known/apple-developer-merchantid-domain-association*
