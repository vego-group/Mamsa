# ── Vue SPA — PRODUCTION build ───────────────────────────────────
# Loaded automatically by `vite build` (production mode). Points the static
# build at the live API. Public URL only — no secrets.
VITE_API_BASE_URL=https://api.mamsaa.com/api/v1
