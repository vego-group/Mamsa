# Root env consumed by docker-compose.yml (variable substitution only).
# Backend Laravel config lives in backend/.env.
APP_ENV=local
APP_KEY=
APP_URL=http://localhost:8080
PORT=8080

DB_DATABASE=mamsa
DB_USERNAME=mamsa
DB_PASSWORD=secret
DB_ROOT_PASSWORD=rootsecret

FRONTEND_DOMAIN=localhost
